package entities.game;

import "entities/game/base/player.proto";
import "entities/auth/account.proto";

entity MatchMaker {
	client MatchReady(entities.game.base.Player player);
	server FindMatch(entities.auth.Account account) returns;
}