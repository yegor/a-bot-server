package entities.system;

import "entities/system/handle.proto";

message Outer {
  required entities.system.Handle h = 1;
  repeated entities.system.Handle hh = 2;
}

entity Test {
  server Outgoing() returns Outer;
}